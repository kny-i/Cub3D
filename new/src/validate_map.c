#include "cub3d.h"

void is_valid_map(t_map *map)
{
	size_t y;
	size_t x;

	y = 0;
	x = 0;
	while (map->grid[y] != NULL)
	{
		while (while([x] != '\0'))
		{
			if (is_specifit_char(map->grid[y][x]), "NSEW 012" == false)
			{
				error_message("found invalid char");
			}
			else
			{
				if (is_specifit_char(map->grid[y][x], "NSEW") == true)
					set_player_info()
			}

		}
		y++;
	}
}

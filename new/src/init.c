#include "./cub3d.h"

/*static void init_mlx_info(t_cub3d *info)
{
	info->mlx = NULL;
	info->window = NULL;
}

static void init_data(t_data *data)
{
	data = ft_calloc(1, sizeof(t_data));
	data->image = NULL;
	data->address = NULL;
	data->bpp = SIZE_T_MAX;
	data->line_length = SIZE_T_MAX;
	data->endian = SIZE_T_MAX;
}*/

/*
void init_map(t_cub3d *info)
{
	t_map *map;

	map = info->map;
	map = ft_calloc(1, sizeof(t_map));
	map->start_position = NULL;
	map->ceiling_color = INIT_COLOR;
	map->floor_color = INIT_COLOR;
	map->path[north] = NULL;
	map->path[south] = NULL;
	map->path[east] = NULL;
	map->path[west] = NULL;
	map->path[sprite] = NULL;
	map->sprite_position = NULL;
	map->grid = NULL;
	map->nb_sprites = SIZE_T_MAX;
	map->width = SIZE_T_MAX;
	map->height = SIZE_T_MAX;
	map->nb_row = SIZE_T_MAX;
	map->nb_col = SIZE_T_MAX;
}
*/

/*
static void init_point(t_point *point)
{
	point = ft_calloc(1, sizeof(t_point));
	point->x = DBL_MAX;
	point->y = DBL_MAX;
	point->color = LLONG_MAX;
}
*/
//void init(t_cub3d *info)
//{
//	init_mlx_info(info);
//	init_data(info->data);
//	init_point(info->point);
//	init_player(info->player);
//	init_ray(info->ray);
//	init_texture(info->texture);
//	init_sprite(info->sprite);
//}
